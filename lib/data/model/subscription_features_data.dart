class SubscriptionFeature {
  const SubscriptionFeature({
    required this.text,
  });

  final String text;
}

final List<SubscriptionFeature> featuresDataList = [
  const SubscriptionFeature(text: 'Unlimited access to all features'),
  const SubscriptionFeature(text: 'Access to modify theme'),
  const SubscriptionFeature(text: 'Adverts on your header profile'),
  const SubscriptionFeature(text: 'Priority customer support'),
];
